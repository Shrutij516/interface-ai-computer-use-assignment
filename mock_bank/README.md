# mock_bank

Target application: a deliberately legacy-styled mock banking app (plain
HTML tables, no test IDs, minimal semantic markup). Per
`interface-ai-project-spec.md` §1, three pages:

1. Member search
2. Member detail (shows savings balance)
3. Open sub-account form + confirmation screen

Not yet implemented — skeleton only.
