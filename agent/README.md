# agent

Discovery module: the LLM-driven observe → decide → act loop that drives
the mock bank app to accomplish a natural-language goal (spec §2, §4). Uses
the accessibility tree as the primary signal, screenshots as secondary
(spec §2).

Not yet implemented — skeleton only.
